rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEST MODE - Allow all read/write
    // ⚠️ WARNING: Only use for development/testing
    // ⚠️ DO NOT use in production!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
